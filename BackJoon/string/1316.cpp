#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main() {
    
    vector<char> excludeChar;

    int numCount;
    cin >> numCount;
    while (numCount--) {
        string targetStr;
        cin >> targetStr;
        for (int strIndex = 0; strIndex < targetStr.size(); strIndex++) {
            char curChar = targetStr[strIndex];
        }
    }

    

    return 0;
}